'use strict';

const {realExpect, hereafter, captureError} = require('./util');

describe('test runner compatibilty', () => {

  xit('should invoke provided done method', () => {
  });

  xit('should return a function which runs a test', () => {
    
  });

});

