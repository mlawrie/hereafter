'use strict';

const {realExpect, hereafter, captureError} = require('./util');

describe('stacktraces', () => {

  xit('should point to expectation', () => {
  });

  xit('should include real trace below', () => {
  });

  xit('should point to when block', () => {});
  
});

